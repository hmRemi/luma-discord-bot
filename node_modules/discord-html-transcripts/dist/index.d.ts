/// <reference types="node" />
import { GenerateSource, GenerateFromMessagesOpts, CreateTranscriptOptions, ValidTextChannels } from './types';
/**
 * @example
 * const discordTranscripts = require('discord-html-transcripts');
 * // or (if using typescript) import * as discordTranscripts from 'discord-html-transcripts';
 *
 * const messages = someWayToGetMessages(); // Must be Collection<string, Message> or Message[]
 * const channel  = someWayToGetChannel();  // Used for ticket name, guild icon, and guild name
 *
 * // You do not need to await this
 * const attachment = discordTranscripts.generateFromMessages(messages, channel);
 *
 * channel.send({
 *     files: [attachment]
 * });
 */
export declare const generateFromMessages: (messages: GenerateSource, channel: ValidTextChannels, opts?: GenerateFromMessagesOpts | undefined) => Promise<string | Buffer | import("discord.js").MessageAttachment>;
/**
 * @example
 * const discordTranscripts = require('discord-html-transcripts');
 * // or (if using typescript) import * as discordTranscripts from 'discord-html-transcripts';
 *
 * const channel = message.channel; // or however you get your TextChannel
 *
 * // Must be awaited
 * const attachment = await discordTranscripts.createTranscript(channel);
 *
 * channel.send({
 *     files: [attachment]
 * });
 */
export declare const createTranscript: (channel: ValidTextChannels, opts?: CreateTranscriptOptions | undefined) => Promise<string | Buffer | import("discord.js").MessageAttachment>;
