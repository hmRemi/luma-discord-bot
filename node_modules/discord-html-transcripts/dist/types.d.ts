/// <reference types="node" />
import type { Collection, Message, DMChannel, PartialDMChannel, TextBasedChannel, MessageAttachment } from 'discord.js';
export declare type ReturnTypes = 'buffer' | 'string' | 'attachment';
export declare type ObjectType<T> = T extends "buffer" ? Buffer : T extends "string" ? string : T extends "attachment" ? MessageAttachment : MessageAttachment;
export declare type GenerateFromMessagesOpts = {
    /**
     * @deprecated Please use returnType instead
     */
    returnBuffer?: boolean;
    returnType?: ReturnTypes;
    fileName?: string;
    minify?: boolean;
    saveImages?: boolean;
    useCDN?: boolean;
};
export declare type GenerateSource = Collection<string, Message> | Message[];
export declare type CreateTranscriptOptions = GenerateFromMessagesOpts & {
    limit?: number;
};
export declare type internalGenerateOptions = {
    returnBuffer?: boolean;
    returnType?: ReturnTypes;
    fileName?: string;
    minify?: boolean;
    saveImages?: boolean;
    useCDN?: boolean;
};
export declare type ValidTextChannels = Exclude<TextBasedChannel, DMChannel | PartialDMChannel>;
export declare type Class<T> = new (...args: any[]) => T;
